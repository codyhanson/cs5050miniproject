we can install spec2006 from our ISO file.

#mkdir /mnt/spec
#mkdir /usr/spec
#mount -o loop /path/to/iso/SPECCPU2006.iso /mnt/spec
#/mnt/spec/install.sh

specify /usr/spec as the directory to install to.
or some other dir that we will point the python script o
